function Reverse(arr){
    var newArr=[];
    for (var i = arr.length-1; i >= 0; i--){
        newArr[i] = arr[i];
        console.log(arr[i]);
    }
    console.log(newArr[i]);
}
Reverse(['a','b','c']);
