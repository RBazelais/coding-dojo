cd Github/Coding-Dojo/Python/myEnvironments/DjangoFirstTry/
source djangoEnv/bin/activate
django-admin startproject projectname
    cd projectname
        mkdir apps
            cd apps
                touch __init__.py
                django-admin startapp appname
                cd appname
                    touch urls.py
                    cd .. to apps
                
# open settings.py
    INSTALLED_APPS = [
        'apps.appname', #add your app
        'django.contrib.admin',
        'django.contrib.auth',
        'django.contrib.contenttypes',
        'django.contrib.sessions',
        'django.contrib.messages',
        'django.contrib.staticfiles',
]

# open urls.py in projectname
    from django.conf.urls import url, include 
    from django.contrib import admin

    urlpatterns = [
        url(r'^$', include('apps.appname.urls')) 
    ]

# open urls.py in appname
    # import your views and set up the url

    from django.conf.urls import url
    from . import views

    urlpatterns = [
        url(r'^$', views.index),
    ]

# open your views.py in appname
    #make the views default
    from __future__ import unicode_literals
    from django.shortcuts import render, HttpResponse, redirect

    def index(request):
        response = "Your Python is working!"
        return HttpResponse(response)

    (optional)
    def reset(request):
        request.session.flush()
        return redirect('/')

cd .. to apps
python ../manage.py runserver               
python ../manage.py migrate

inside appname
make static folder
put style.css inside templates folder
make templates folder
put index.html inside templates folder

# back to views.py
    # point index to html
    return render(request, "index.html")